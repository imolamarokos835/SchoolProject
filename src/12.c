#include <stdio.h>
int main() {
    int age = 18;
    if (age > 21) {
        printf("You are old enough to drink!\n");
    } else {
        printf("You are not yet old enough to drink.\n");
    }
    return 0;
}
