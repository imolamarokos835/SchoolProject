#include <stdio.h>
#include <stdlib.h>

int main() {
  int age = 15;
  if (age > 18) {
    printf("You are old enough to drink.\n");
  } else {
    printf("You are not old enough to drink.\n");
  }
  return 0;
}
